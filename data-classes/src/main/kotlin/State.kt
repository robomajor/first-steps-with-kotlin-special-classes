enum class State {
    IDLE, RUNNING, FINISHED          //Defines a simple enum class with three enum instances.
                                    // The number of instances is always finite and they are all distinct
}